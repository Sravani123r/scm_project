export type SignUpType = {
  email: string;
  userName: string;
  password: string;
  contactNumber: string;
  about: string;
};

export type LoginType = {
  email: string;
  password: string;
};

export type ForgotPasswordType = {
  email: string;
};
